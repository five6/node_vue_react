import React from 'react';
import ReactDOM from 'react-dom';
import TopicRoot from '../containers/TopicRoot';

const nodeDiv =document.getElementById("topic-container");
ReactDOM.render(
    <TopicRoot />,
    nodeDiv
);
